//COLLADA Animated file reading
http://www.wazim.com/Collada_Tutorial_1.htm

//TBN Matrix
http://www.terathon.com/code/tangent.html

https://www.gamedev.net/articles/programming/general-and-gameplay-programming/the-lua-tutorial-r2999/

//TODO: have a look at angular limits in physics engine, especially the 3D level
//TODO: have a look at collisions in the physics engine, see if it is possible to fine tune them

//DONE: build a finite state machine ai, using lua to store and decide on states
//DONE: build a simple path following ai
//DONE: build a A* pathfinding alogirthm (that does not get stuck)
//TODO: work on the animation model, see if that can be fixed
//TODO: work on a menu system
//TODO: build a animation system (for coins and the like)
//TODO: build methods to control the camera using input
//TODO: Optimise the mesh from assimp, will likly slow down loading but should speed up rendering
//DONE: resource manager to have a count map for number of times a resource is used, only delete once resource is not used anymore
//TODO: look at the lifetime of the audio engine
//TODO: pause and unpause music when in pause menu
//TODO: deal with removing audio in the resource manager when refactoring the audio manager
//TODO: create the render manager
//DONE: build a way to create entities in lua
//TODO: look at when the 'guard' goes back to its post
//TODO: move bone mats out of the rigged animation
//TODO: Turn componenttypes array into vector and do removing / adding in the isystem class
//TODO: Give the option to output log to a file instead of cout

file:///C:/Users/538704/source/repos/Game-Engine/irrKlang-64bit-1.6.0/doc/cpp/index.html#changingSounds

int i;
int top = lua_gettop(luaState);
printf("---- Begin Stack ----\n");
printf("Stack size: %i\n\n", top);
for (i = top; i >= 1; i--)
{
	int t = lua_type(luaState, i);
	switch (t)
	{
	case LUA_TSTRING:
		printf("%i -- (%i) ---- `%s'", i, i - (top + 1), lua_tostring(luaState, i));
		break;

	case LUA_TBOOLEAN:
		printf("%i -- (%i) ---- %s", i, i - (top + 1), lua_toboolean(luaState, i) ? "true" : "false");
		break;

	case LUA_TNUMBER:
		printf("%i -- (%i) ---- %g", i, i - (top + 1), lua_tonumber(luaState, i));
		break;

	default:
		printf("%i -- (%i) ---- %s", i, i - (top + 1), lua_typename(luaState, t));
		break;
	}
	printf("\n");
}
printf("---- End Stack ----\n");
printf("\n");